# Required only for a live discovery run.
OPENAI_API_KEY=
OPENAI_MODEL=

# Local ports used by the target application and operator console.
TARGET_APP_PORT=4310
OPERATOR_PORT=4311
# happy | transient | permission; omitted means the URL query controls the demo scenario.
TARGET_SCENARIO=
